import { StyleSheet, Dimensions } from "react-native";
const styles = StyleSheet.create({


    profile: {
        width: 70,
        height: 70,
        borderRadius: 30,
    }
})
export default styles